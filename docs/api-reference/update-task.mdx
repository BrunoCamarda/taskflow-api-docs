---
title: 'Update a task'
openapi: 'PATCH /tasks/{task_id}'
---

<Note>
  **Partial Updates:** This endpoint supports partial updates. You only need to include the fields you intend to change in the request body; omitted fields will remain unchanged.
</Note>

## Request Specifications

The specification below defines the required payload format and the expected structure for successful responses.