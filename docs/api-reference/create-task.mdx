---
title: 'Create a task'
openapi: 'POST /tasks'
---

<Note>
  **Prerequisite:** Ensure you have a valid `project_id` generated from the `POST /projects` endpoint. Tasks cannot exist without a parent project.
</Note>

## Request Specifications

The specification below defines the required payload format and the expected structure for successful responses.
